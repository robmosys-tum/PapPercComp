// --------------------------------------------------------
// Code generated by Papyrus C++
// --------------------------------------------------------

#define GestureDetectorComponentCompdef_GestureDetectorComponent_BODY

/************************************************************
 GestureDetectorComponent class body
 ************************************************************/

// include associated header file
#include "GestureDetectorComponentCompdef/GestureDetectorComponent.h"

// Derived includes directives
#include "../../src/GestureDetectorComponentCompdef/GestureDetectorComponent_impl.h"
#include "rclcpp/rclcpp.hpp"

// Include from Include declaration (body)
// declare options
rclcpp::NodeOptions gesturedetectorcomponent_options;

int main(int argc, char **argv) {
	rclcpp::init(argc, argv);

	auto gesturedetectorcomponent = std::make_shared
			< GestureDetectorComponentCompdef::GestureDetectorComponent_impl
			> (gesturedetectorcomponent_options);

	RCLCPP_INFO(gesturedetectorcomponent->get_logger(),
			"GestureDetectorComponent has been initialized");

	rclcpp::executors::MultiThreadedExecutor executor;

	executor.add_node(gesturedetectorcomponent->get_node_base_interface());

	executor.spin();
	rclcpp::shutdown();
}

// End of Include declaration (body)

namespace GestureDetectorComponentCompdef {

// static attributes (if any)

/**
 * 
 * @param options 
 */
GestureDetectorComponent::GestureDetectorComponent(
		rclcpp::NodeOptions /*in*/options) :
		rclcpp_lifecycle::LifecycleNode("GestureDetectorComponent", options) {
	points_input_sub_ =
			create_subscription < geometry_msgs::msg::PoseArray
					> ("points_input", rclcpp::QoS(rclcpp::KeepLast(100)).best_effort(), std::bind(
							&GestureDetectorComponentCompdef::GestureDetectorComponent_impl::pointsHandler,
							(GestureDetectorComponent_impl*) this,
							std::placeholders::_1));

	commands_pub_ = create_publisher < std_msgs::msg::String > ("commands", 1);
	// directly activate a publisher
	commands_pub_->on_activate();

}

} // of namespace GestureDetectorComponentCompdef

/************************************************************
 End of GestureDetectorComponent class body
 ************************************************************/
